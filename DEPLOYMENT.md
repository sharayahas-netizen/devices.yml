# مهمة: تنصيب برنامج استبيان فندق القصر على السيرفر

> هذا الملف موجّه لوكيل Claude Code يعمل محلياً (لديه وصول للسيرفر 192.168.1.14).
> البرنامج جاهز ومكتمل ومرفوع على GitHub — المطلوب فقط تنصيبه وتشغيله. لا تعدّل الكود.

## عن البرنامج

نظام استبيان رضا نزلاء لفندق القصر (Al Qasr Hotel): كل غرفة لها رمز QR فريد، النزيل يمسحه من جواله ويقيّم الغرفة والفندق والمطعم (عربي/إنجليزي). بعد التعبئة تُقفل الغرفة 24 ساعة على مستوى قاعدة البيانات فلا يمكن التقييم مرتين. لوحة تحكم عربية على `/admin` فيها الإحصائيات والإجابات وإدارة الغرف وطباعة ملصقات QR وتصدير Excel والإعدادات (تغيير كلمة المرور، رفع الشعار، تعديل مدة القفل).

- **المستودع**: `https://github.com/sharayahas-netizen/devices.yml`
- **الفرع**: `claude/hotel-survey-program-gwjhyn`
- **التقنية**: Node.js + Express + SQLite (يدعم Node 20+: يستخدم `node:sqlite` المدمجة على 22.5+ و`better-sqlite3` تلقائياً على الأقدم — سيرفرنا عليه Node 20.20.2 وهذا مدعوم)
- **المنفذ**: 8090 (مضبوط افتراضياً في الكود وملف systemd ورابط QR)
- **قاعدة البيانات**: ملف `data/survey.db` يُنشأ تلقائياً عند أول تشغيل

## خطوات التنصيب المطلوبة على السيرفر (192.168.1.14)

نصّب في مجلد مستقل `/opt/hotel-survey` — **منفصل تماماً عن باقي الخدمات على السيرفر ولا تلمسها**.

```bash
# ملاحظة: قد يكون المجلد موجوداً من محاولة سابقة (تم فيها clone و npm install
# بنجاح لكن npm run seed فشل لأن الكود القديم كان يتطلب Node 22).
# إن وُجد المجلد: حدّثه بدل إعادة النسخ:
cd /opt/hotel-survey && sudo git pull && sudo npm install

# وإن لم يوجد:
sudo git clone -b claude/hotel-survey-program-gwjhyn https://github.com/sharayahas-netizen/devices.yml.git /opt/hotel-survey
cd /opt/hotel-survey && sudo npm install

# إدخال غرف الفندق الـ58 (آمن لإعادة التنفيذ — لا يكرر الغرف):
sudo npm run seed
# المتوقع: "Seeded 58 units. Rooms now in database: 58"

# التشغيل الدائم عبر systemd:
sudo cp deploy/hotel-survey.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable --now hotel-survey
sudo systemctl status hotel-survey --no-pager
```

## التحقق بعد التنصيب (نفّذه فعلياً)

```bash
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8090/admin/login   # المتوقع 200
curl -s http://localhost:8090/admin/login | grep -o 'لوحة تحكم'              # المتوقع أن تظهر
```

وتأكد أن الجهاز الآخر على الشبكة يصل إلى `http://192.168.1.14:8090` (إن كان هناك جدار ناري مثل ufw، افتح المنفذ 8090).

## توزيع الغرف (للمعلومية — السكربت يدخلها تلقائياً)

| الطابق | الوحدات | العدد |
|---|---|---|
| الأول | 101–112، ثم 114 (الرقم 113 متخطى عمداً) + Royal Room 1 | 14 |
| الثاني | 201–212 | 12 |
| الثالث | 301–314 | 14 |
| الرابع | جناح 401 – جناح 406 | 6 |
| الخامس | 501–512 | 12 |

## أخبر المستخدم في النهاية بهذه الخطوات اليدوية المتبقية له

1. فتح `http://192.168.1.14:8090/admin` — كلمة المرور المؤقتة: `admin123`
2. **تغيير كلمة المرور فوراً** من صفحة الإعدادات
3. رفع شعار فندق القصر من صفحة الإعدادات (يظهر في الاستبيان وملصقات QR)
4. فتح صفحة «رموز QR» وطباعة الملصقات الـ58 وتعليق كل ملصق في غرفته

## استكشاف الأخطاء

- **`Cannot find module 'node:sqlite'`**: الكود قديم — نفّذ `sudo git pull && sudo npm install` (الإصلاح موجود في آخر commit).
- **المنفذ 8090 محجوز**: عدّل `Environment=PORT=` في `/etc/systemd/system/hotel-survey.service` لمنفذ آخر، ثم `daemon-reload` و`restart`، وعدّل «رابط السيرفر» في إعدادات لوحة التحكم لنفس المنفذ (هذا الرابط يدخل في رموز QR).
- **مشاكل صلاحيات على `data/`**: الخدمة تعمل بمستخدم root افتراضياً؛ إن غيّرت المستخدم في ملف الخدمة فأعطه ملكية المجلد `sudo chown -R USER /opt/hotel-survey`.
- البيانات كلها في `data/survey.db` — انسخه دورياً كنسخة احتياطية.
